# 视觉认知脑电项目协作约定

- 修改 `run_q1.m` 或后续分析代码后，先用 `data/` 中的 MAT 文件完整运行，检查退出状态、输出表和图，再提交 Git commit 并推送到 GitHub。若因认证或网络无法推送，应明确报告本地提交号和阻塞原因，不得声称已上传。
- 每次提交在说明中写明修改内容、运行命令、MATLAB 版本、关键结果及未解决问题。结果有变化时同步更新 `results_summary.md` 和 `README.md`。
- 仓库包含 `data/` 中的原始 MAT、`problem/` 中的题目文档和 `results/` 中的完整分析输出。使用 `DATA_PROVENANCE.md` 的哈希核对原始数据版本；后续新数据或身份信息的发布须单独核对来源和授权，认证凭据不得提交。
- 不把 Fz/F3/F4 的额区晚期正波直接认定为典型中央—顶区 P300，也不在未通过验证时声称左右三角形可稳定区分。A/B 仅称跨记录，除非取得独立证据证明是不同受试者。
- 保留训练/测试边界：标准化、模型拟合和任何调参只用训练数据；有多个候选模型时报告全部比较与多重检验，避免按测试结果挑选最佳模型。
