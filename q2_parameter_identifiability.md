# Q2 参数分类与可辨识性分析

## 1. 目标

Q2 只有 F3、Fz、F4 三个头皮通道，且尚未有个体 MRI、真实 lead field 或直接 LGN/V1 记录。因此参数分类首先服务于“哪些参数可以固定、哪些只能给先验边界、哪些才允许从完整 ERP 估计”，而不是追求一套看似精确的生理数字。

分类含义：

- **Class I — Literature-fixed / design-fixed**：文献或实验设计足以固定结构/规范；本数据不应重新估计。
- **Class II — Literature-bounded**：机制或一般生理文献提供方向/边界，但所给四篇论文未给出本题人类数值；只能作为有界先验或敏感性分析。
- **Class III — Data-fitted**：在固定结构、固定规范化和留出验证下，完整 waveform 对其有足够敏感性，才允许估计。

“未由所给文献确定”不是缺失填补许可；在补充针对性人类文献之前不得编造范围。

## 2. 参数清单

| 参数/符号 | 含义与单位 | 来源与证据 | 范围/约束 | 分类 | 当前处理建议 | 主要风险 |
|---|---|---|---|---|---|---|
| `q∈{L,R}`、通道顺序 `[F3,Fz,F4]` | 条件和观测列定义；无量纲/列名 | 题目与 Q1 接口 | 离散固定集合 | I | 固定，不拟合 | 列顺序或标签混乱会改变空间解释 |
| `p(t)`、刺激 onset/offset | 已知刺激时间包络；秒 | 实验事件标记，非四篇论文参数 | 由事件表和 epoch 定义确定 | I | 由事件表确定，固定 | 事件延迟与神经延迟混淆 |
| `φ_L,φ_R` 的维度与范数 | 刺激特征编码；无量纲 | Stringer 支持分布式/高维编码；二维实现是 D 级简化 | 主分析 `dim=2` 且 `||φ_q||₂=1`；`Δφ` 范围待数据可辨识性检查 | I（结构/范数）；III（若估计相对方向） | 先固定二维并归一化；只允许估计受限的 `Δφ` | `φ` 与输入增益、`W_L` 完全相乘 |
| `d_L,d_C,K` | LGN、皮层状态和宏观源数；无量纲 | 三通道观测约束；不是文献直接参数 | `d_L=1,d_C=1`；`K∈{1,2}`，`K=3` 仅敏感性分析 | I（每个候选模型内） | 对每个候选值独立比较复杂度 | 源数增加会让模型非唯一 |
| `L∈R^{3×K}` | source→F3/Fz/F4 mixing；相对幅度或无量纲 | Breakspear 要求 forward model；无本题个体 lead field | `||L_:k||₂=1`；列符号固定；元素生理范围未由四篇论文确定 | II（结构约束）/III（相对列方向） | 仅估计相对混合 | 与源幅度 `H_C/α` 尺度不可分 |
| `τ_L,τ_C,τ_E,τ_I`（s） | 有效动力学时间常数 | Breakspear 说明状态变量/参数；四篇论文没有本题人类数值 | **未由所给文献确定**；待补充人类视觉动力学文献后设界 | II | 先用外部文献边界或敏感性网格；强正则且跨 task 共享 | 与延迟、输入脉冲宽度、低通增益补偿 |
| `δ_L`（s） | 刺激/丘脑输入延迟 | Lien 的 42±8 ms 是 2-Hz 刺激相位差，非通用传导延迟；四篇论文未给人类范围 | **未由所给文献确定**；`δ_L≥0` | II | 采用外部文献边界或预设网格 | `δ_L` 与 `τ_L`、事件标记误差相互替代 |
| `W_L`、`B_C`、`g_E/g_I` | 输入/丘脑到皮层增益；相对幅度 | Lien 支持“丘脑驱动被皮层放大”（B）；无本题数值 | 符号由模型定义；数值范围未由四篇论文确定 | II→III（只估计一个合并增益） | 将 `W_LB_C` 或单一 `g_in` 作为可估对象，避免两层同时自由 | input gain × cortical gain 不可分 |
| `A_C` 或 `w_{EE},w_{EI},w_{IE},w_{II}` | 线性耦合或 E/I 耦合；按模型参数化 | Breakspear 支持 Wilson–Cowan/neural mass 结构（C），无 task-specific 参数 | Model B 要求稳定矩阵；Model C 固定兴奋/抑制符号，数值范围待补充文献 | II（稳定性/符号）→III（少量有效组合） | 优先拟合阻尼/有效增益而非全矩阵 | 高维补偿、多稳态、局部最优 |
| `S` 的形状、斜率和阈值 | sigmoid transfer；斜率/阈值依参数化 | Breakspear 说明 sigmoid 的群体平均意义；未给本题值 | 固定 logistic/tanh 族；斜率、阈值范围未由四篇论文确定 | I（形状族）/II（斜率范围） | 固定形状；斜率和阈值不同时自由估计 | 斜率与耦合/输入增益共线 |
| `b_L,b_C,b_E,b_I,d` | baseline/偏置；与输出单位一致 | Q1 baseline 与任务状态；动物文献不提供人类 EEG baseline | 由 Q1 预刺激段和训练数据约束 | III（但应分层共享） | 从预刺激段估计或固定；task 只允许少量偏置差 | baseline 会伪装成左右或 task 效应 |
| `H_C` 或 `α_E,α_I` | 皮层状态→宏观源幅度；相对 EEG 单位 | 宏观源代理是 C/D 级假设；无直接文献参数 | 固定一个合并系数或设置 unit-source 规范；绝对范围不可识别 | II（结构）/III（合并源幅度） | 固定一个源幅度规范或只拟合合并系数 | 与 `L` 的尺度完全不可分 |
| `φ_R-φ_L` 的幅度 | Left/Right 输入差异；无量纲 | Stringer/Lien 提供机制方向，不证明本题左右稳定差异 | 包含 0；上界由归一化和训练数据预注册 | III（受限） | 共享方向、跨记录层级随机/固定效应 | 过拟合微弱差异，违反 Q1 的不稳定性证据 |
| task gain `g_τ`、阻尼调制 `Δw_τ` | Task1/Task2 状态调制；无量纲/相对量 | Horrocks 支持状态依赖群体动力学（B）；没有本题数值 | 以 shared value 为中心；范围由训练数据敏感性和强正则确定 | III（少量） | 一次只开放一组调制 | task 调制与 condition/record 随机效应混淆 |
| 噪声协方差 `Σ_ε` | EEG 观测噪声；`µV²` 或 Q1 单位平方 | Q1 bootstrap/残差估计 | 对称半正定；数值由训练残差/bootstrap 约束 | III（观测层） | 从训练记录估计并固定到验证阶段；可用对角/收缩协方差 | 噪声模型错误会改变模型排序 |

## 3. 关键结构不可辨识性

### 3.1 源幅度与 lead field 尺度

若

\[
J'(t)=aJ(t),\qquad L'=L/a,
\]

则 `L'J'=LJ`。因此三通道 waveform 无法同时唯一确定源幅度和 lead-field 绝对尺度。主模型固定每个 `L` 列的二范数为 1，并固定列符号（例如使 Fz 投影为正）；源幅度由 `H_C` 或 `α` 的一个合并系数承担。

### 3.2 输入增益与皮层增益

在 Model B/C 中，`W_L`、`B_C`、`g_E/g_I` 常以乘积影响输出。若同时自由估计，可能得到等价波形的不同参数。策略是固定 `||φ_q||`，把输入—皮层通路压缩成一个有效增益 `g_in`，或固定 `W_L` 后仅估计 `B_C`。

### 3.3 时间常数与延迟

一个较慢的低通时间常数和一个较大的纯延迟都可能改变峰潜伏期/相位。三通道 ERP 对二者的区分很弱。策略是：先用事件锁定的 stimulus onset、外部文献边界和敏感性网格约束 `τ_L,δ_L`；如仍需估计，只释放一个而共享另一个，不允许每个 record/task 独立估计。

### 3.4 E/I 参数与 sigmoid 斜率

耦合系数、阈值、sigmoid 斜率和输入增益可共同改变振幅、阻尼和稳态。仅凭三通道宏观波形不能恢复完整的四个 `w` 和两个 sigmoid 参数。主版本应固定 sigmoid 形状/斜率，拟合有效阻尼和有效增益；只有存在独立的多时间尺度证据时才增加 E/I 自由度。

### 3.5 源数与混合矩阵

当 `K>1` 时，源置换、旋转和相互补偿均可能保留相同的三通道输出。`K=1` 是主模型；`K=2` 用固定的列范数、符号和最小复杂度惩罚比较；`K=3` 仅作为上限敏感性分析，不能解释为识别出三个独立脑区。

## 4. 建议的可辨识性审查

在真正拟合前，应对每个候选模型执行以下审查：

1. **局部灵敏度**：计算预测 waveform 对参数的数值 Jacobian，检查列秩和条件数。
2. **profile likelihood / profile loss**：逐个固定参数，其余参数重新优化，观察是否存在平坦区间。
3. **模拟恢复**：在固定参数的合成 waveform 上加入与 Q1 相当的噪声，检查是否能恢复参数而非只恢复输出。
4. **跨记录/跨 task 验证**：参数拟合只用训练记录，验证集只用于模型比较；不得按测试波形挑选结构。
5. **消融一致性**：检查去掉 LGN、E/I、左右编码或 task gain 后，误差增加是否稳定而非只出现在单条记录。

## 5. 本阶段结论

- 当前四篇论文足以支撑“低维输入—丘脑驱动—皮层群体动力学—forward observation”的结构，但不足以给出一套人类 Q2 生理参数。
- 绝对源幅度、个体 lead field、完整 E/I 耦合矩阵和人类 LGN 延迟不得作为已知量。
- 首选可辨识参数化为：固定输入编码范数、固定/规范化 lead field 列、共享时间尺度、单一有效输入增益、少量 task modulation，以及观测噪声协方差。
- 任何“模型拟合很好”的结论都必须同时报告参数不确定性、复杂度和留出 waveform 表现，不能只报告训练误差或分类准确率。
