# Q1 滤波器实现审查

审查范围：现行 `run_q1.m` 的滤波器设计、连续数据处理顺序、边界处理与理论响应。本文件是 **Phase 1 实现审查**，不是四种高通频率在真实数据上的效果比较，也不据此选定最终高通。数值由本机 MATLAB R2024a（`24.1.0.2537033`）对代码所用 `butter`／`filtfilt` 组合做只读核查；源数据单位尚未确定。

## 1. 当前实现的精确流程

| 环节 | 代码与含义 |
|---|---|
| 输入 | 每份连续记录的 `data(1:3,:)`，分别对应 Fz、F3、F4；采样率由输入检查限定为 256 Hz。 |
| 高通 | `butter(2, highPassHz/(fs/2), 'high')`：**2 阶数字 IIR Butterworth**。主分析 `highPassHz=0.5` Hz；已有诊断为 0.1、0.5、1.0 Hz。0.2 和 0.25 Hz 尚未写入现行代码。 |
| 低通 | `butter(4, 30/(fs/2), 'low')`：**4 阶数字 IIR Butterworth**，正式值 30 Hz。 |
| 执行 | `filtfilt(bl,al,filtfilt(bh,ah,raw'))'`：先高通双向，再低通双向；矩阵转置后时间在第一维，三个通道各自独立处理。是**连续记录先滤波，再按提示起点切 epoch**，不是逐 epoch 滤波。 |
| 主 epoch | `round(0.2×256)=51` 个提示前采样点至 `round(0.8×256)=205` 个提示后采样点，即实际约 −0.1992 至 +0.8008 s。 |
| 基线 | 滤波并切段**之后**，对每个试次、每个电极减去 `time<0` 的均值；共 51 个采样点（−0.1992 至 −0.0039 s）。 |
| 现有诊断 epoch | 0.1／0.5／1.0 Hz 诊断使用约 −0.8008 至 +0.8008 s 的切段，但基线仍为约 −0.2 至 0 s。扩展窗口不是滤波 padding。 |

代码锚点：`run_q1.m` 的 `default_config`（约第 148–168 行）、`make_epochs`（约第 268–295 行）及 `filter_sensitivity`（约第 458–490 行）。原始幅值的截断、峰峰值和跳变判断使用**未滤波**的原始 epoch，滤波后才得到用于估计的 ERP 波形。

## 2. 截止频率、幅频和相频

`butter` 中的频率按奈奎斯特频率 128 Hz 归一化：四个候选高通传入的归一化值分别为 `0.00078125`、`0.0015625`、`0.001953125`、`0.00390625`；30 Hz 低通为 `0.234375`。指定 cutoff 是**单向** Butterworth 的 −3.010 dB 幅值点；经 `filtfilt` 后，高通和低通各自的幅值响应平方（可理解为高通、低通的等效幅值阶次分别加倍），因此各自 cutoff 处是约 **−6.021 dB**（幅值约 0.5），不能将输出理解成“名义 cutoff 处只衰减 3 dB”。

远离整段记录边界时，级联的等效频率响应为

`H_eff(f)=|H_HP(f)|² × |H_LP(f)|²`。

下表是完整两级双向滤波器的**幅值增益（dB）**，经 MATLAB R2024a 的 `freqz` 计算；四列仅改变高通，低通固定 30 Hz。

| 频率 Hz | HP 0.10 Hz | HP 0.20 Hz | HP 0.25 Hz | HP 0.50 Hz |
|---:|---:|---:|---:|---:|
| 0.05 | −24.61 | −48.20 | −55.93 | −80.00 |
| 0.10 | −6.02 | −24.61 | −32.05 | −55.93 |
| 0.20 | −0.53 | −6.02 | −10.73 | −32.06 |
| 0.25 | −0.22 | −2.98 | −6.02 | −24.61 |
| 0.50 | −0.01 | −0.22 | −0.53 | −6.02 |
| 1.00 | <−0.01 | −0.01 | −0.03 | −0.53 |
| 2.00 | <−0.01 | <−0.01 | <−0.01 | −0.03 |
| 30.00 | −6.02 | −6.02 | −6.02 | −6.02 |

理想无限长、稳态意义下 `H_eff(f)` 为非负实数：**有非零增益的频点相位为 0 rad，群延迟为 0**；直流处高通增益为零，相位无定义。因此四种高通没有不同的理论峰时“相位延迟”。但是零相位是**非因果双向滤波**的结果，并不保证瞬态不变：截止频率附近的幅值会被不同程度改变，强瞬态前后都可能出现回响，形态、峰幅和峰时仍可能改变。有限记录两端的反射与初值也不具有严格的全记录线性时不变响应。

## 3. `filtfilt` 补边与冲激响应

现行代码未在 epoch 上做滤波，因此**没有“每个 epoch 的 padding 长度”**。MATLAB R2024a 的 `filtfilt.m` 在每次对整段连续记录调用时，采用以端点为中心的奇对称反射，并计算初始状态以减少首尾瞬态；传递函数系数形式中反射长度为 `3×filter_order`：2 阶高通每端 **6 个样本（23.44 ms）**，4 阶低通每端 **12 个样本（46.88 ms）**。这两个长度是端点补边参数，**不是** IIR 冲激响应的有效长度。这里是两次独立 `filtfilt` 调用，分别处理各自输入的整段记录端点。实现依据为 MATLAB R2024a `filtfilt.m` 中的 `nfact=max(1,3*(nfilt-1))` 与端点反射分支。

IIR 冲激响应在数学上没有严格有限支持，所以“实际长度”必须先定义阈值。本审查在 120 s 长的零背景中间放置单位冲激，使用与正式代码**完全相同**的高通→低通双向调用，计算围绕冲激中心包含绝对冲激响应面积（`Σ|h|`）95%、99%、99.9% 的最小双侧半宽。下表中的半宽不是滤波器的硬截断或安全隔离距离。

| 高通 Hz | 95% 半宽 s | 99% 半宽 s | 99.9% 半宽 s | 99.9% 对应约总跨度／采样点 |
|---:|---:|---:|---:|---:|
| 0.10 | 3.703 | 8.457 | 13.672 | 27.34 s／7001 点 |
| 0.20 | 1.863 | 4.238 | 6.855 | 13.71 s／3511 点 |
| 0.25 | 1.492 | 3.395 | 5.492 | 10.98 s／2813 点 |
| 0.50 | 0.758 | 1.707 | 2.766 | 5.53 s／1417 点 |

这是**完整 2 阶高通＋4 阶低通、两级分别双向**后的结果。0.1 Hz 的时间扩展比 0.5 Hz 长得多；反过来说，0.1 Hz 是否更保真仍须结合真实数据及半模拟恢复，不能由扩展长度单独判定。

## 4. 边界效应、回响与扩展窗口是否足够

- **记录首尾**：四份源 MAT 的首个提示距记录开头约 20.05–20.16 s；最后提示加上正式 0.8 s 分析窗后，距记录末尾约 22.41–22.87 s。即使诊断窗前伸至约 −0.8 s，首个诊断片段距起点仍至少约 19.25 s。相对 0.1 Hz 冲激绝对面积 99.9% 半宽 13.67 s，首尾滤波启动／终止瞬态**预计不是正式分析窗的主要威胁**，但没有逐试次量化边界误差，不能声称边界污染严格为零。
- **epoch 边缘**：因为连续记录已先滤波，epoch 起点和终点本身不会触发新的 `filtfilt` 启动瞬态。因此“−0.8 到 +0.8 s 扩展窗口够不够当 padding”不适用于当前实现；若未来改成逐 epoch 滤波，0.8 s 对 0.1 Hz 显然不充分。当前诊断扩展窗口可帮助观察较长的刺激前趋势，但不能隔断滤波器从窗口外带入的信息。
- **记录内部的强瞬态**：双向滤波会将尖峰、阶跃或饱和附近的扰动传播到**其前后**。0.1 Hz 的 99% 半宽约 8.46 s，与四份记录约 8.05–9.28 s 的相邻提示间隔同量级；目标事件也约在提示后 2.2 s，处于此响应范围内。这意味着后续目标相关波动、相邻试次信号或伪影可能以不同强度回传到提示锁定窗口；实际幅度取决于事件形状和振幅，须用真实数据检验，不能由半宽推算污染量。原始幅值 QC 主要检查本试次 −0.2 至 +0.8 s，不能自动排除来自更远位置的滤波回响。
- **pre-/post-ringing**：理论冲激响应关于事件中心近似双侧对称，含低频长尾；大瞬态前出现扰动是零相位滤波的固有可能，不等于存在真实的刺激前神经反应。当前未做逐事件的实测回响归因，不能断言已有 ERP 图中的某个前刺激偏移一定由滤波、慢漂移或真实预期活动造成。
- **基线解释**：现行基线校正会使每条输出 trial 在所选 51 点的**算术均值按构造为零**。即便如此，不同条件的刺激前**斜率、RMS、MAD、较早的 −0.8 至 −0.2 s 片段**仍可不同。基线相减也会把预刺激差异作为常数带入整条后刺激曲线。刺激前的条件差既可能来自慢漂移，也可能来自零相位滤波对后刺激事件的预回响，需独立对照，不能仅凭负时间差异区分来源。

## 5. 与后续四频率敏感性实验的接口

现有 `filter_sensitivity` 在主分析的 `accepted` 基础上，又因扩展到约 −0.8 s 而排除扩展片段内任一通道原始绝对值达到 999 的试次；因此现有 `results/filter_sensitivity.csv` 的样本集为 **333/400**（A Task1 78、A Task2 88、B Task1 85、B Task2 82），不是主分析的 **350/400**。该附加规则不随高通频率变化，所以旧表内部频率对比仍使用固定试次，却不能与 350 次主分析混为同一试次集。本轮四频率比较如果按要求固定全部 350 次，应保留它们并对扩展段异常单独标记；若为某项诊断另设更严格集合，必须以不同名称及逐组数量报告，不能声称所有分析均为同一 350 次。

本审查只能解释**滤波器能改变什么**：0.1 到 0.5 Hz 会强烈重加权约 0.1–0.5 Hz 的信号成分，并通过后滤波基线相减影响 250–500 ms 的测量。它不能从频响单独判定 A Task2 F3 的 45.56→8.63 源单位变化中，哪些是真实慢 ERP、慢漂移、事件间交叠或强瞬态回响。该归因需在固定事件／试次／基线／估计器条件下，结合完整 12 组合的真实数据与独立半模拟恢复结果完成。

与新脚本输出对应时要保持时间窗名称准确：`q1_highpass_real.m` 的 `baseline_rms/mad/variance` 是**正式 −0.1992 至 −0.0039 s 校正后片段**的所有保留试次样本汇集统计；`pre_stimulus_delta_rms` 是**完整扩展提示前 −0.8008 至 −0.0039 s** 的逐点 Huber 右减左差波 RMS；较早扩展段（−0.7969 至 −0.2031 s）另由 `baseline_mean_left/right`、`baseline_difference` 和 `baseline_slope_*` 描述。这三类指标不应统称“较早基线 RMS”。

## 6. 补充：后续 target/click 起点邻域的有界反事实检查

为检验上文的“后续事件可向前回传”是否足以解释主要数值变化，独立的 `q1_future_event_leakage.m` 在**复制的**三通道连续信号中，对每次 target/click 起点 ±0.15 s 或 ±0.30 s 的片段做两端点线性插值，再按相同的高通→30 Hz 低通双向滤波、相同的 350 个提示试次、相同的 −0.2 至 0 s 基线、逐点 Huber（δ=1.345，15 次）和 λ=12 二阶平滑重算。源 MAT、事件、左右标签、保留集合均未改。这里只测试 0.1 与 0.5 Hz，以及局限于事件起点邻域的影响，不是新高通选择试验。

| A Task2 F3，右减左 250–500 ms，源单位 | 0.1 Hz | 0.5 Hz |
|---|---:|---:|
| 原始 350 试次，λ=12 拟合曲线 | 61.3336 | 18.5376 |
| target+click ±0.15 s 插值后的变化 | −0.2051 | −0.0082 |
| target+click ±0.30 s 插值后的变化 | +0.0709 | −0.0876 |

在全部 12 个“记录×任务×电极”组合上，±0.30 s 方案对 λ=12 右减左窗均值的**最大绝对变化**为 0.3390（0.1 Hz）与 0.2073（0.5 Hz）源单位；±0.15 s 方案分别为 0.2051 与 0.0236。A Task2 F3 在固定 350 试次上的原始频率差为 42.7960 源单位，±0.30 s 替换后为 42.9545，变化约 0.1585。逐点 Huber、平滑结果及所有组合/方案均在 `results_highpass/q1_future_event_leakage.csv`。这些 350 试次数据不可与旧版扩展窗 333 试次的 45.56／8.63 直接作同样本数值对照。

因此，**target/click 起点附近 ±0.30 s 内的大瞬态，按此反事实定义，不能解释主要的高通敏感性**。但线性插值本身会改变原信号，且未覆盖事件后的长时神经/眼动/动作相关响应、较早持续漂移或其他伪影；它既不是干净生理信号的真值，也不能证明后续事件整体没有回传。此检查只为该局部窗口的敏感性设定一个可复核的经验界限。
