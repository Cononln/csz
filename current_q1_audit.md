# Q1 现行方法审查与升级决策

审查对象：`run_q1.m`、`README.md`、`results_summary.md`、`results/*.csv` 和四份源 MAT 的通道标签。审查基于仓库当前 `main` 的静态代码与已保存输出，**本阶段没有改动、重新运行或调参 `run_q1.m`**。数值是源文件单位，不能标成 µV。A/B 只表示记录 A/B，不推定受试者身份。文献证据及适用边界见 `paper_method_mapping.md`。

## 审查结论

**当前 Q1：需要明显修改评价与参数依据，但主体预处理和 Huber 响应估计可保留。** 目前能提供四组提示锁定的额区观测曲线；尚不能证明 250–500 ms 的左右差是稳定视觉特征，更不能据此定位经典中央–顶叶 P300。最严重的方法学风险是低频滤波选择明显改变慢波和左右差，而当前主展示的 0.5 Hz 与正则化强度缺乏针对 ERP 保真的客观选择；没有眼电/眼动参考也无法确定后期共同波或残余条件差的来源。

## 逐项核对：代码实际行为

| 要求核对项 | 实际实现与证据 | 判定 |
|---|---|---|
| 1 当前滤波器 | `make_epochs` 对连续的原始第 1–3 通道先高通后低通：`butter(2,0.5 Hz,'high')` 和 `butter(4,30 Hz,'low')`，见 `run_q1.m:270–273`；不是 FIR。 | 保留架构，需重新评价高通。 |
| 2 阶数 | 高通二阶、低通四阶是单程 Butterworth 设计阶数；各调用 `filtfilt`，双程等效幅度响应更陡，不能简单在论文中称“二阶整体带通”。 | 修改表述。 |
| 3 相位 | 两次 `filtfilt` 为非因果零相位；作用于整段连续记录后才截取 epoch，避免逐 epoch 滤波边界成为每次事件的伪迹（`270–295`）。 | 保留。 |
| 4 event onset | `cueStart=find(cue~=0 & [true,cue(1:end-1)==0])`，所以只取 0→非零起点，不会把 0.2 s 的所有非零点当事件（`198–212`）；仅硬查 100 个事件，没有显式验证事件值域为 ±1 或外部触发器延迟。 | 保留并加 QC。 |
| 5 epoch | `preSec=0.2`、`postSec=0.8`（`148–150`），索引用 `round(preSec*256)`、`round(postSec*256)`，实际采样点约 `−0.1992…0.8008 s`，257 个点（`279–295`）。 | 保留，精确写采样点边界。 |
| 6 baseline | `time<0` 的 51 个预提示采样点取均值并逐电极减去（`281,293–295`）。敏感性分析用扩展 `−0.8…0.8 s`，但基线仍 `−0.2…0 s`（`462–489`）。没有回归基线法。 | 保留为比较基准，检测漂移敏感性。 |
| 7 artifact rejection | 用原始 epoch 任一通道绝对值 ≥999 记“疑似截断”，再用三个通道中最大的整 epoch 峰峰值和最大相邻采样跳变筛除；超边界也剔除（`289–306`）。它不识别眨眼、眼动、肌电的生理类别。 | 保留强异常筛查，修改命名和验证。 |
| 8 MAD 阈值 | 在每份记录中，对 `inBounds & ~clipped` 的试次，阈值=`median+6×1.4826×MAD`，峰峰值与最大跳变分别计算；左右条件共用该记录阈值（`299–313`）。四份记录的峰峰阈值约 670.10、1538.99、1317.30、1599.64；跳变阈值约 60.62、59.22、71.09、55.20，均为源单位。`6` 未做 SME/恢复误差选择。 | 保留稳健尺度思想，修改阈值依据。 |
| 9 Huber 实现 | 各记录×左右×电极单独处理；每个采样点的 trial 中位数作初值，逐时间点固定 `1.4826×MAD` 尺度，IRLS 权重 `min(1,δσ/|x−位置|)`，加权均值至收敛或 15 轮（`315–353`）。没有整个 trial 的连续质量权重。 | 保留为 Method B。 |
| 10 Huber δ | `huberConstant=1.345`（`156`）；这是常见稳健估计常数，但本数据尚无调参或消融证据。 | 暂保留，列入预注册敏感性。 |
| 11 平滑正则 | 先求 Huber 曲线 `m`，后独立求 `(I+λD₂ᵀD₂)⁻¹m`，其中 `D₂=[1,-2,1]`（`325–326,355–359`）。它不是 trial 级损失与正则共同优化；未按采样间隔归一化。 | 保留为对照，修正方法描述。 |
| 12 λ 选择 | `smoothLambda=12` 为配置常数（`158`），代码未见 GCV、交叉验证、半模拟选择或峰值保真测试。已有 250–500 ms 窗内，输出拟合曲线相对 Huber 曲线的条件均值变化很小（抽查最大约 0.03 源单位），但峰幅/潜时影响未检验。 | 修改：先用保真评价判定是否需要平滑，再调 λ。 |
| 13 左/右分开 | `accepted & cueDir==−1/+1` 分别形成普通平均、Huber 和拟合曲线（`319–336`）；窗口和差波单独输出（`362–397`）。 | 保留。 |
| 14 Task1/Task2 分开 | 四文件逐一处理，每条记录保留 `task` 标记；跨记录仅同 Task 的 A/B 互测（`24–25,40–112,616–649`）。 | 保留。 |
| 15 A/B 混合 | 没有跨 A/B 合并求单条 ERP。`records` 分别存四份；同任务互测用于辅助解码，没有把 A/B 当受试者组（`99–115,616–649`）。 | 保留。 |
| 16 训练/测试泄漏 | LDA 的均值、标准差、类均值和协方差都只在训练折计算（`706–740`），跨记录模型只由训练记录拟合（`623–637`），未见直接使用测试标签调模型。然而同记录五折前先用整份记录的无标签试次计算 MAD 剔除阈值和接受集合（`50–54,299–306,659–665`），故不是完全独立的归纳式测试流程。跨记录测试记录也自行估质量阈值；这更像预处理使用未标记测试批数据的转导式设置，应如实声明或在未来严格测试中锁定阈值。 | 修改评价协议。 |
| 17 classification 地位 | 分类是后段附加输出，`README.md` 和 `results_summary.md` 已明确将其称为辅助；主处理仍输出 ERP 曲线和时间窗统计（`79–96,114–143`）。没有代码按分类准确率选择滤波/Huber/λ；但当前文档还缺以 ERP 保真为主的跨方法定量比较。 | 保持辅助地位，新增主评价。 |

`DataLabel` 实际显示第 1–3 行是 Fz/F3/F4，第 4–6 行是 Decon，第 8 行是 `VisCue:L-1/R+1`。代码只加载 `DataLabel`、不逐行校验其内容（`45,171–187`），后续版本宜将通道标签核对变成断言。第 7 行 ECG 未用；没有独立 EOG。事件与试次表保留了每次提示与质量标记，适合后续复核。

## 已有结果能支持什么

| 记录 | 左/右提示 | 左/右保留 | 总保留 | 条件差波分半平均相关 |
|---|---:|---:|---:|---:|
| A Task1 | 54 / 46 | 41 / 39 | 80/100 | −0.052 |
| A Task2 | 57 / 43 | 51 / 42 | 93/100 | 0.499，分半分位区间跨 0 |
| B Task1 | 48 / 52 | 41 / 46 | 87/100 | −0.289 |
| B Task2 | 50 / 50 | 44 / 46 | 90/100 | 0.032 |

表源：`results/quality_summary.csv`、`results/contrast_split_half.csv`。`results/split_half_reliability.csv` 的单条件曲线相关多为正且 Task2 很高，但这可以由左右共有的晚期波驱动；不能替代差波保真。逐条件剔除数不同，且是否与刺激、事件阶段或慢波强度有关未检验。`highPeak`、`highJump` 与 `rawClipped` 可能重叠，不能把各列直接求和当剔除总数。

`results/filter_sensitivity.csv` 已做高通 0.1/0.5/1.0 Hz、低通固定 30 Hz 的对照，并对扩展窗口重新检查截断。因此敏感性表在每个滤波参数下使用相同的扩展窗合格试次，但其样本数可少于主分析。A Task2 F3 的右减左 250–500 ms 均值由 0.1 Hz 的约 45.56 变为 0.5 Hz 的约 8.63，再到 1.0 Hz 的约 3.83 源单位。这是需要解释和报告的低频敏感性，而非选择“漂亮曲线”的依据。现有敏感性尚未覆盖 0.25 Hz 高通及 20/40 Hz 低通；也没有对峰时、峰幅和条件差的同试次误差作完整比较。

`results/within_record_decoding.csv` 的两套特征在四记录上五折平衡准确率约 0.36–0.56，循环移位检验的八项 Holm 校正 p 均为 1；`results/cross_record_decoding.csv` 的 A/B 互测约 0.41–0.52，亦无稳健证据。这个结果不能反向证明 ERP 不存在，只表明当前特征/检验未验证稳定左右可分性。三通道共同波、额区晚期正波的神经与眼动来源无法由现有数据唯一识别。

## 四类决策

### 【保留】

- 使用原始 Fz/F3/F4，不把 Decon 当主要信号；VisCue 的 0→±1 起点及任务/记录/左右分层。
- 连续记录滤波再分段、预提示基线、强异常剔除、普通平均 + Huber 平均双轨输出。
- 当前二阶差分曲线作为 Method B 的展示对照；保留时间留出和 A/B 互测，但将其放在辅助验证层。
- 固定随机种子、逐试次事件与质量表、源数据不改写；空间对比 `F3−F4` 和 `Fz−(F3+F4)/2` 只作为重参考特征。

### 【修改】

- 主高通 0.5 Hz 的依据及频率敏感性：预先限定 0.1、0.25、0.5 Hz 为主要候选，1.0 Hz 为诊断性压力测试；20/30/40 Hz 低通只做少量有理由的搭配，不做为了显著性而挑选的全组合搜索。固定试次后检查形态、均值、峰、峰时、左右差、分半。
- 6×MAD、绝对值 999、Huber δ=1.345 和 λ=12：区分设备截断证据与经验阈值；用已知恢复目标、trial-level SME 和消融验证，不让最终测试标签参与参数选择。
- 试次剔除要报告各条件剔除比例及原始幅值/基线分布；为分类的严格测试锁定训练阶段 QC 规则，或明确其“无标签测试批预处理”性质。
- `results_summary.md` 中“0.55–0.80 s 共同波动”的讨论保持描述性，不把所有共同活动当噪声；对缺少眼电参考的混淆明确说明。

### 【删除】

- 暂不删除可运行的 Huber 主体、二阶拟合或现有输出。未来正式升级若需要移除某指标，应先在同一试次集上证明其不增加 ERP 保真或可靠性。
- 论文表述中应避免把 `fittedCurve` 的平滑效果写成 `window_statistics.csv` 的 250–500 ms 估计改进；两者代码路径不同。

### 【新增】

- Method A：保守滤波、事件/基线、简单预注册强异常阈值、算术均值。Method B：现行 Huber + 当前平滑。Method C：待验证的质量加权鲁棒正则估计。三者使用同一原始文件、事件、划分及可比较试次集；分别另报告“保留更多试次”的实际收益。
- 比较估计器时先锁定同一滤波器、时间窗与试次集合，只改变平均/拟合方法；比较完整处理流程时另设一组分析，并同时报告各法的剔除率与有效样本量。否则，滤波、试次数和估计器的收益无法归因。
- trial QC 最小集先试平线/疑似饱和、预刺激 RMS/MAD、最大跳变；检查相关性后再考虑整段峰峰值、极端幅值占比、条件内模板偏离。模板必须训练数据内构建且交叉拟合，避免把真实左右差误判成坏试次。连续权重由稳健标准化和预设单调映射产生，参数用训练/验证试次选择。
- 评价分三层：①预刺激 RMS、异常幅值、保留数和可能的残余伪影代理；②同记录 trial-level SME/Bootstrap、单条件及右减左分半、均值/峰/潜时稳定性；③左右差波幅值、相关和空间对比。分类仍是辅助，不按其筛选清理法。没有眼电，任何“眨眼已去净”指标均只能表述为代理。
- 半模拟：从训练部分构建条件参考模板；评估部分使用不重叠的相对高质量 trial，保留其未污染副本作为配对的操作性恢复目标，再向副本注入漂移、瞬态尖峰、振幅突发、基线偏移和彩色/高斯背景噪声。伪影幅值、时长与污染率由真实数据的训练部分估计；参考模板和未污染副本都不等于绝对生理真值。按记录×Task×左右分层，训练集定参数、冻结后在独立污染集比较 RMSE、相关、峰幅/峰时/250–500 ms 均值和右减左恢复误差，并设置“把真实响应磨平”失败检查。可增加在预提示背景片段中注入已知模板的验证，明确区别其人工真值与真实 ERP。
- 运行日志中保存参数、MAT 哈希、事件与剔除原因、每 trial 权重、有效样本量 `n_eff=(Σw)²/Σw²`、随机种子、所有方法指标和输出图表；未来实现放新目录，不覆盖现行可复现结果。

## Phase 3：三个候选路线与推荐

| 路线 | 要解决的真实问题 | 文献支撑与差异 | 代价/失败条件 | 当前建议 |
|---|---|---|---|---|
| A. 文献化 QC + 保留现行 Huber | 当前滤波/剔除/平滑依据弱，缺主评价；但未证明估计器本身失效。 | P1/P2 支持逐阶段 QC 与半模拟；P4 支持“剔除是否净改善 SME”；P3 支持保真与伪影双目标。 | 方法改进主要在验证设计，可将现有 Huber + 二阶正则完整形式化。 | **必做基准和保底结果**。 |
| B. 质量加权 Huber + 二阶正则联合估计 | 目前硬剔除和逐点 Huber 无法反映“整条 trial 边缘质量”，λ 也与噪声尺度脱节。 | 四篇论文支持 QC、稳健评价和已知信号恢复，但**没有直接验证该联合目标函数**；这是需实证检验的模型设计。 | w 与 Huber 可能重复；权重可偏置条件差；λ 可抹峰。须做 `B`、`B+w`、`B+联合λ` 等消融与独立半模拟。 | **推荐作为受控挑战方法**，只有保真与净精度改善才升级为主方法。 |
| C. 逐通道小波纠正 + 当前 Huber | 对保留试次但仍含局部短暂伪影的情况，HAPPILEE 的逐通道小波有物理可行性。 | P1/P2 有低密度小波和模拟支持，但优化数据/参数不等于本题三通道、256 Hz、额区慢 ERP；P3 的 wICA 不是此法。 | 可能损伤低频慢波、条件差，增加多个小波参数；需独立验证且成本较高。 | 暂列探索性备选，不作默认升级。 |

对路线 B，建议目标函数按每个 `记录×Task×左右×电极` 分层写为 `min_s { (1/Σ_i w_i) Σ_i w_i Σ_t ρδ[(x_i(t)−s(t))/σ(t)] + λ‖D₂s‖² }`。相对于现有代码，它新增的是**trial 级连续质量信息与联合求解**，而不是又做一次普通 Huber。权重 `w_i∈[0,1]` 由候选质量量的 robust z-score 单调映射；`σ(t)`、δ、λ 的训练阶段估计和尺度/时间网格规范必须明确。若低质量权重极低，有效样本量会下降；若质量指标来自 ERP 主效应时间窗，还可能系统性压低真实右/左响应，所以优先以预提示基线和平线/饱和等非目标窗指标构建权重。权重与 Huber 的边际收益、峰值/潜时损伤和计算收敛应由消融及独立注入实验回答，不能从公式美观推断。当前 `window_statistics.csv` 的 250–500 ms 数值来自逐试次窗均值的 Huber 估计，不是平滑后的 `fittedCurve`；因此 λ 对该现有指标的影响应记为“不适用”，对未来从拟合曲线提取的峰幅、峰时及窗均值则必须实测。

**建议执行次序**：先完成 A 的独立验证基座，保持 B 为当前正式结果；随后将路线 B 作为 Method C，在冻结的评估协议下检验。若 Method C 未在恢复误差、trial-level 测量精度和左右差保真上获得一致、可重复的净收益，最终保留 Method B，并报告 Method C 没有显著改进。这个决策不应按分类准确率或某一滤波设置的单次显著性改写。路线 C 只有在剩余局部伪影明确成为瓶颈时再研究。

## 当前是否足以支撑 Q1 写作

**足以写“原始三通道提示锁定观测响应与局限”的初版，不足以写“最佳去伪影方法”“左右差已可靠保留”或“经典 P300 机制已证实”。** 对完整的 Q1 方法论论证，最值得优先增加的是一个独立试次的半模拟恢复实验，并用与最终指标对应的 SME/差波保真联合评价当前 Huber 与新方法。此前不应重写正式代码或宣称新方法胜出。
